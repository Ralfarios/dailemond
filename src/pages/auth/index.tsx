import { FC } from 'react';

export const AuthPage: FC = () => {
  return (
    <main>
      <section>This is Auth Page, let's say I will use this as login and register page</section>
    </main>
  );
};
